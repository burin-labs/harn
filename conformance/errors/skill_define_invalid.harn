// skill_define rejects non-string values for keys that must be strings
// (e.g. `invocation`). This catches typos like passing a list by mistake.
pipeline test(task) {
  let _ = skill_define(skill_registry(), "bad", {invocation: ["not", "a", "string"]})
}
