// Phase C: missing-variant `match` is a hard error, not a warning.
// `Edit` is unmatched and there is no wildcard arm.
enum Action {
  Create,
  Edit,
  Delete,
}

pipeline test(task) {
  let a = Action.Create
  match a.variant {
    "Create" -> { log("c") }
    "Delete" -> { log("d") }
  }
}
