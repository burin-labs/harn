// Whole-module (wildcard) import brings in the library's `pub` value surface:
// `pub const` / `pub let` bindings become available by name, same as `pub fn`.
import "pub_const_lib"

pipeline test(harness: Harness, task) {
  harness.stdio.println(MAX_RETRIES)
  harness.stdio.println(GREETING)
  harness.stdio.println(scaled())
}
