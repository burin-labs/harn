import { GitStatusReceipt, git_ls_remote } from "std/git"

fn wrong_receipt(process: HarnessProcess, remote: string) -> GitStatusReceipt {
  return git_ls_remote(process, remote)
}

pipeline default(harness: Harness) {
  harness.stdio.println(wrong_receipt(harness.process, "origin").success)
}
