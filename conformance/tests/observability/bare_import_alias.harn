import { obs } from "observability"

pipeline default(harness: Harness) {
  const o = obs(harness)
  o.reset()
  o.configure({backend: o.Backend.test()})
  o.log("alias")
  harness.stdio.println(o.events_take()[0].payload.message)
}
