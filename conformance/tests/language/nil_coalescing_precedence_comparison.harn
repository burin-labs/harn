pipeline test(task) {
  const classified = false
  const fixture = {expect_missing_dep: nil}
  __io_println(classified == (fixture.expect_missing_dep ?? false))
  __io_println((classified == fixture.expect_missing_dep) ?? false)
  const snap = nil
  __io_println((snap?.total_iterations ?? 0) < 2)
}
