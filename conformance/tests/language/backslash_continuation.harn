pipeline default(harness: Harness) {
  // Backslash continuation works for any operator
  // After formatting, + already supports multiline natively
  const msg = "hello"
    + " world"
  harness.stdio.println(msg)
  // Verify basic arithmetic still works
  const x = 10 - 3
  harness.stdio.println(x)
}
