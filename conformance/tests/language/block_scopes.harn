pipeline default() {
  const outer = "outer"
  if true {
    const branch = "if-branch"
    __io_println(branch)
  } else {
    const branch = "else-branch"
    __io_println(branch)
  }
  for item in [1, 2] {
    const loop_label = "loop-${item}"
    __io_println(loop_label)
  }
  try {
    throw "boom"
  } catch (err) {
    __io_println(err)
  }
  __io_println(outer)
}
