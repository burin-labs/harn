pipeline default() {
  const op = {type: "read_file", name: "test"}
  __io_println(op.type)
  __io_println(op.name)
  const cfg = {match: "pattern", for: "user", enum: "status"}
  __io_println(cfg.match)
  __io_println(cfg.for)
  __io_println(cfg.enum)
  const gates = {request_approval: true, ask_user: "owner", dual_control: 2, escalate_to: "frontier"}
  __io_println(gates.request_approval)
  __io_println(gates.ask_user)
  __io_println(gates.dual_control)
  __io_println(gates.escalate_to)
  const flow = {finally: "cleanup", require: "policy", select: "ready", skill: "review", eval_pack: "smoke"}
  __io_println(flow.finally)
  __io_println(flow.require)
  __io_println(flow.select)
  __io_println(flow.skill)
  __io_println(flow.eval_pack)
  __io_println(json_stringify({eval_pack: "smoke"}))
}
