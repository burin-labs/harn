pipeline default() {
  let op = {type: "read_file", name: "test"}
  println(op.type)
  println(op.name)
  let cfg = {match: "pattern", for: "user", enum: "status"}
  println(cfg.match)
  println(cfg.for)
  println(cfg.enum)
  let gates = {request_approval: true, ask_user: "owner", dual_control: 2, escalate_to: "frontier"}
  println(gates.request_approval)
  println(gates.ask_user)
  println(gates.dual_control)
  println(gates.escalate_to)
  let flow = {finally: "cleanup", require: "policy", select: "ready", skill: "review", eval_pack: "smoke"}
  println(flow.finally)
  println(flow.require)
  println(flow.select)
  println(flow.skill)
  println(flow.eval_pack)
  println(json_stringify({eval_pack: "smoke"}))
}
