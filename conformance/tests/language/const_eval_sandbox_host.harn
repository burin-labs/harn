// Bounded const-eval — issue #1791. Touching `harness.*` from a const
// initializer must fail at typecheck with the dedicated sandbox-violation
// diagnostic, never reach the runtime.
const Z = harness.clock.now()

pipeline main() {
  log(Z)
}
