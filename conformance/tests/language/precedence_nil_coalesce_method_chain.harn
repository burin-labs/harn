fn entries() {
  return [
    {language: "Rust"},
    {id: "ts", language: "TypeScript"},
    {language: "Python"},
    {id: "go", language: "Go"},
  ]
}

fn find_match(name) {
  const normalized = name.lower()
  for entry in entries() {
    if (entry?.id ?? entry?.language ?? "").lower() == normalized {
      return entry?.language
    }
  }
  return "not found"
}

pipeline test(harness: Harness, task) {
  harness.stdio.println(find_match("ts"))
  harness.stdio.println(find_match("python"))
  harness.stdio.println(find_match("go"))
  harness.stdio.println(find_match("rust"))
  harness.stdio.println(find_match("missing"))
}
