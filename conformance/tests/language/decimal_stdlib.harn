pipeline test(harness: Harness, task) {
  // Decimal flows through min/max/sign/round exactly (was silently nil).
  harness.stdio.log(min(decimal("19.99"), decimal("9.99")))
  harness.stdio.log(max(decimal("19.99"), decimal("9.99")))
  harness.stdio.log(sign(decimal("-2.5")))
  harness.stdio.log(sign(decimal("0")))
  harness.stdio.log(sign(decimal("4.2")))
  harness.stdio.log(round(decimal("2.4")))
  // sum() accumulates decimals exactly, promoting int operands.
  harness.stdio.log([decimal("1.50"), decimal("2.50"), decimal("0.99")].sum())
  harness.stdio.log([decimal("10"), 5, decimal("0.50")].sum())
}
