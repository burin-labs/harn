pipeline test(harness: Harness, task: unknown) {
  /* this is a comment */
  const x = 42
  harness.stdio.log(x)
  /* outer /* nested */ still comment */
  const y = "survived"
  harness.stdio.log(y)
}
