pipeline test(harness: Harness, task: unknown) {
  fn add1(x: int) {
    return x + 1
  }
  fn double(x: int) {
    return x * 2
  }
  const result = 3 |> add1 |> double
  harness.stdio.log(result)
}
