pipeline default() {
  let fallback = nil
    ?? "fallback"
  println(fallback)
  let same = 1
    == 1
  println(same)
  let different = 1
    != 2
  println(different)
  let smaller = 1
    < 2
  println(smaller)
  let larger_or_equal = 2
    >= 2
  println(larger_or_equal)
}
