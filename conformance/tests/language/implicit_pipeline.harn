// Test: top-level code executes without a pipeline block
const x = 1 + 2

harness.stdio.println(x)

fn double(n) {
  return n * 2
}

harness.stdio.println(double(5))

const items = [1, 2, 3]

for item in items {
  harness.stdio.print("${item} ")
}

harness.stdio.println("")
