pipeline default() {
  // Render with bindings
  let result = render("conformance/tests/fixtures/greeting.prompt", {name: "Alice", place: "Harn"})
  println(result)
  // Render without bindings (just reads file)
  let raw = render("conformance/tests/fixtures/greeting.prompt")
  println(raw)
}
