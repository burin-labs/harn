pipeline test(harness: Harness, task) {
  // Test progress builtin in standalone mode.
  progress("scan", "Scanning files")
  progress("enrich", "Enriching directory", {current: 3, total: 10})
  harness.stdio.println("done")
}
