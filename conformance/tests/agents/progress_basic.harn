pipeline test(harness: Harness, task) {
  // Test progress builtin in standalone mode.
  harness.stdio.progress("scan", "Scanning files")
  harness.stdio.progress("enrich", "Enriching directory", {current: 3, total: 10})
  harness.stdio.log("done")
}
