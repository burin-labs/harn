import { agent_session_drain_bridge_injections } from "std/agent/state"

pipeline main() {
  let result = agent_session_drain_bridge_injections("bridge-remind-conformance", "interrupt_immediate")
  __io_println(result.delivered)
  __io_println(result.reason)
}
