import { agent_session_drain_bridge_injections } from "std/agent/state"

pipeline main(harness: Harness) {
  const result = agent_session_drain_bridge_injections(harness, "bridge-remind-conformance", "finish_step")
  harness.stdio.println(result.delivered)
  harness.stdio.println(result.reason)
}
