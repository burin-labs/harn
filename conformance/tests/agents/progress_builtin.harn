pipeline default(harness: Harness, task) {
  // Basic progress
  harness.stdio.progress("phase1", "Starting work")
  // Progress with numeric percentage
  harness.stdio.progress("phase2", "Halfway there", 50)
  // Progress with progress/total
  harness.stdio.progress("phase3", "Processing files", 3, 10)
}
