pipeline default(harness: Harness, task: unknown) {
  harness.stdio.progress("download", "Fetching artifacts", {mode: "spinner", step: 1})
  harness.stdio.progress("index", "Indexing files", {mode: "bar", current: 3, total: 5, width: 10})
}
