pipeline test(task) {
  var cleanup_ran = false
  try {
    try {
      throw "inner"
    } catch (e) {
      throw "rethrown"
    } finally {
      cleanup_ran = true
    }
  } catch (e2) {
    log("outer: ${e2}")
  }
  log("cleanup_ran: ${cleanup_ran}")
}
