pipeline test(task) {
  let result = trigger_test_harness("dead_man_switch_alerts_on_silent_binding")
  println(result.ok)
  println(len(result.alerts) == 1)
  println(result.alerts[0].binding_id == "deadman.fixture")
  println(result.details.silent_for_ms == 300000)
}
