pipeline test(task) {
  const result = trigger_test_harness("rate_limit_throttles")
  __io_println(result.ok)
  __io_println(len(result.emitted) == 2)
  __io_println(result.attempts[1].backoff_ms == 60000)
  __io_println(result.details.throttled_for_ms == 60000)
}
