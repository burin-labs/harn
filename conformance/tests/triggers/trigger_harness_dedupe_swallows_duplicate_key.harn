pipeline test(task) {
  let result = trigger_test_harness("dedupe_swallows_duplicate_key")
  println(result.ok)
  println(len(result.emitted) == 1)
  println(result.emitted[0].note == "first_delivery")
  println(result.details.first_claim && !result.details.second_claim)
}
