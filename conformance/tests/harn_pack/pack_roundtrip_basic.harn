import "../_common"

pipeline default() {
  let repo_root = exec("git", "rev-parse", "--show-toplevel").stdout.trim()
  let harn_bin = env_or("HARN_CONFORMANCE_HARN_BIN", resolve_harn_bin(repo_root))
  let root = path_join(temp_dir(), "harn_pack_basic_" + uuid_v7())
  mkdir(root)
  write_file(path_join(root, "hello.harn"), "__io_println(\"hi\")\n")
  let pack = exec_at(
    root,
    harn_bin,
    "pack",
    "hello.harn",
    "--unsigned",
    "--out",
    path_join(root, "hello.harnpack"),
    "--json",
  )
  __io_println(pack.success)
  let pack_data = json_parse(pack.stdout)
  __io_println(pack_data.schemaVersion >= 1)
  __io_println(pack_data.ok)
  __io_println(starts_with(pack_data.data.bundle_hash, "blake3:"))
  __io_println(pack_data.data.signature.present)
  __io_println(file_exists(path_join(root, "hello.harnpack")))
  let verify = exec_at(
    root,
    harn_bin,
    "pack",
    "verify",
    path_join(root, "hello.harnpack"),
    "--allow-unsigned",
    "--json",
  )
  __io_println(verify.success)
  let verify_data = json_parse(verify.stdout)
  __io_println(verify_data.ok)
  __io_println(verify_data.data.bundle_hash == pack_data.data.bundle_hash)
  __io_println(verify_data.data.signature_present)
  __io_println(verify_data.data.signature_verified)
  __io_println(verify_data.data.module_count)
}
