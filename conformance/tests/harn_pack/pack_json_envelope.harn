import "../_common"

pipeline default() {
  let repo_root = exec("git", "rev-parse", "--show-toplevel").stdout.trim()
  let harn_bin = env_or("HARN_CONFORMANCE_HARN_BIN", resolve_harn_bin(repo_root))
  let root = path_join(temp_dir(), "harn_pack_json_" + uuid_v7())
  mkdir(root)
  write_file(path_join(root, "hello.harn"), "println(\"json\")\n")
  let pack = exec_at(
    root,
    harn_bin,
    "pack",
    "hello.harn",
    "--unsigned",
    "--out",
    path_join(root, "hello.harnpack"),
    "--json",
  )
  println(pack.success)
  let pack_data = json_parse(pack.stdout)
  println(pack_data.ok)
  // schemaVersion is monotonically bumped; assert it's at least the
  // baseline that shipped with #1781.
  println(pack_data.schemaVersion >= 2)
  println(starts_with(pack_data.data.bundle_hash, "blake3:"))
  println(pack_data.data.signature.algorithm)
  let verify = exec_at(
    root,
    harn_bin,
    "pack",
    "verify",
    path_join(root, "hello.harnpack"),
    "--allow-unsigned",
    "--json",
  )
  println(verify.success)
  let verify_data = json_parse(verify.stdout)
  println(verify_data.ok)
  println(verify_data.schemaVersion >= 1)
  println(verify_data.data.bundle_hash == pack_data.data.bundle_hash)
}
