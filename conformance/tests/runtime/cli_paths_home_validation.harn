import { xdg_config_home } from "std/cli/paths"

fn main(_harness: Harness) {
  const message = try {
    xdg_config_home(harness, "harn")
    "ok"
  } catch (e) {
    to_string(e)
  }
  harness.stdio.println(message == "std/cli/paths: HOME or USERPROFILE must be an absolute path")
}
