pipeline default() {
  // Render with bindings
  const result = render("fixtures/greeting.prompt", {name: "Alice", place: "Harn"})
  __io_println(result)
  // Render without bindings (just reads file)
  const raw = render("fixtures/greeting.prompt")
  __io_println(raw)
}
