pipeline test(task) {
  fn count_up(start, end) {
    let i = start
    while i < end {
      yield i
      i = i + 1
    }
  }
  // Test .next() method
  const gen = count_up(1, 4)
  const r1 = gen.next()
  __io_println(r1.value)
  __io_println(r1.done)
  const r2 = gen.next()
  __io_println(r2.value)
  __io_println(r2.done)
  const r3 = gen.next()
  __io_println(r3.value)
  __io_println(r3.done)
  // After exhaustion
  const r4 = gen.next()
  __io_println(r4.value)
  __io_println(r4.done)
  // Test for-in with generator
  for n in count_up(10, 13) {
    __io_println(n)
  }
  // Test yield with no value
  fn empty_yields() {
    yield
    yield
  }
  const g2 = empty_yields()
  const e1 = g2.next()
  __io_println(e1.value)
  __io_println(e1.done)
}
