pipeline test(harness: Harness, task: unknown) {
  fn show(_x: string, y: any = "default") {
    harness.stdio.log(y)
  }
  // Explicit nil should NOT trigger the default
  show("a", nil)
  // Omitting should trigger the default
  show("a")
}
