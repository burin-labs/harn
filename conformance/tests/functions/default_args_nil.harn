pipeline test(harness: Harness, task) {
  fn show(_x, y = "default") {
    harness.stdio.log(y)
  }
  // Explicit nil should NOT trigger the default
  show("a", nil)
  // Omitting should trigger the default
  show("a")
}
