pipeline test(harness: Harness, task) {
  fn greet(name, greeting = "hello") {
    harness.stdio.println("${greeting}, ${name}!")
  }
  greet("world")
  greet("world", "hi")
  fn add(a, b = 10) {
    return a + b
  }
  harness.stdio.println(add(5))
  harness.stdio.println(add(5, 20))
  fn flag(x, enabled = true) {
    if enabled {
      harness.stdio.println("on: ${x}")
    } else {
      harness.stdio.println("off: ${x}")
    }
  }
  flag("test")
  flag("test", false)
}
