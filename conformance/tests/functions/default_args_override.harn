pipeline test(harness: Harness, task) {
  fn make_config(host = "localhost", port = 8080, debug = false) {
    return "${host}:${port} debug=${debug}"
  }
  harness.stdio.println(make_config())
  harness.stdio.println(make_config("example.com"))
  harness.stdio.println(make_config("example.com", 3000))
  harness.stdio.println(make_config("example.com", 3000, true))
}
