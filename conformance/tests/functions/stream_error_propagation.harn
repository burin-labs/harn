pipeline test(task) {
  gen fn broken() -> Stream<int> {
    emit 1
    throw "stream failed"
  }
  try {
    for n in broken() {
      __io_println(n)
    }
  } catch (err) {
    __io_println("caught ${err}")
  }
}
