pipeline default() {
  // pub fn is parsed and works
  /** multiply. */
  pub fn multiply(a, b) {
    return a * b
  }
  fn divide(a, b) {
    return a / b
  }
  __io_println(multiply(6, 7))
  __io_println(divide(42, 6))
}
