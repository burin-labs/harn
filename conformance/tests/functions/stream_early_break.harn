pipeline test(harness: Harness, task: unknown) {
  gen fn many() -> Stream<int> {
    emit 1
    emit 2
    emit 3
  }
  for n in many() {
    harness.stdio.println(n)
    break
  }
  harness.stdio.println("after break")
}
