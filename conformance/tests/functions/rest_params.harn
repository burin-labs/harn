pipeline test(harness: Harness, task) {
  fn sum(...nums) {
    let total = 0
    for n in nums {
      total += n
    }
    return total
  }
  harness.stdio.println(sum(1, 2, 3))
  harness.stdio.println(sum(10))
  harness.stdio.println(sum())
  // Rest param with leading required params
  fn greet(greeting, ...names) {
    for name in names {
      harness.stdio.println("${greeting}, ${name}!")
    }
  }
  greet("Hello", "Alice", "Bob")
  // Rest param is always a list
  fn check_type(...args) {
    harness.stdio.println(type_of(args))
  }
  check_type()
}
