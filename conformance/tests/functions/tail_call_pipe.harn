pipeline default(harness: Harness) {
  fn process(n: int) {
    if n <= 0 {
      return "done"
    }
    return n - 1 |> process
  }
  // Would overflow without TCO
  harness.stdio.println(process(10000))
}
