pipeline default() {
  fn apply(f: fn(int) -> int, x: int) -> int {
    return f(x)
  }
  const double: fn(int) -> int = { x -> x * 2 }
  __io_println(apply(double, 5))
  __io_println(apply({ x -> x + 10 }, 3))
}
