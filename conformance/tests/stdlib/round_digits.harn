pipeline test(harness: Harness, task) {
  // 1-arg form is unchanged: nearest integer, halves away from zero.
  harness.stdio.log(round(2.5))
  harness.stdio.log(round(-2.5))
  harness.stdio.log(round(2.4))
  harness.stdio.log(round(7))
  // 2-arg form: round floats to N decimal places (half away from zero).
  harness.stdio.log(round(2.567, 2))
  harness.stdio.log(round(2.5, 0))
  harness.stdio.log(round(-2.565, 1))
  harness.stdio.log(round(1.25, 1))
  harness.stdio.log(round(-1.25, 1))
  // Values produced by binary float arithmetic round to the intended
  // decimal (0.1 + 0.2 is 0.30000000000000004 before rounding).
  harness.stdio.log(round(0.1 + 0.2, 2))
  harness.stdio.log(round(2.675 + 0.005, 2))
  // Negative digits round to tens / hundreds buckets — for floats and ints.
  harness.stdio.log(round(1234.5678, -2))
  harness.stdio.log(round(1250, -2))
  harness.stdio.log(round(-1250, -2))
  harness.stdio.log(round(1249, -2))
  harness.stdio.log(round(7, 3))
  // Extreme digit counts degrade gracefully instead of overflowing.
  harness.stdio.log(round(1.5, 400))
  harness.stdio.log(round(1.5, -400))
  harness.stdio.log(round(123456789, -19))
  // Decimal keeps the decimal type (and its banker's midpoint rule).
  harness.stdio.log(round(decimal("2.675"), 2))
  harness.stdio.log(round(decimal("2.665"), 2))
  harness.stdio.log(round(decimal("1250"), -2))
}
