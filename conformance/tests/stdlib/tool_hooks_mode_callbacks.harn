import { lifecycle_audit_log_take } from "std/lifecycle"
import {
  tool_hooks_mode_deny_with_explanation,
  tool_hooks_mode_passthrough_only_audit,
  tool_hooks_mode_rewrite_with_audit,
} from "std/tool_hooks"

/**
 * Tool-hook mode callbacks side effects (TH-03 #1896).
 *
 * Each `tool_hooks_mode_*` callback emits a lifecycle audit entry
 * (and, for the rewrite mode, also queues a `tool_rewritten` system
 * reminder). This fixture invokes each mode directly against a
 * synthesized match record + `inner` echo executor and drains the
 * lifecycle audit log between cases so the per-mode side effects can
 * be asserted independently.
 */
pipeline default() {
  /* Clear any latent audit entries from earlier in the run so each
   * mode's drain starts from a clean slate. */
  lifecycle_audit_log_take()
  let echo = { args -> return {ok: true, ran: args.command} }
  /* Synthesize a rewrite-bearing match record matching the shape
   * `tool_hooks_match` produces. Using an inline match avoids the
   * full preset_run_command path so the side-effect plumbing is the
   * subject under test. */
  let rewrite_rule = {
    rule_id: "rust.cargo.target_dir",
    catalogue_id: "harn-canon/rust",
    severity: "warning",
    explanation: "use --target-dir to avoid lockfile thrash",
    references: [],
    rewrite: { command, _ctx -> command + " --target-dir target-shared" },
  }
  let rewrite_result = tool_hooks_mode_rewrite_with_audit(rewrite_rule, {command: "cargo build --release"}, echo)
  println(rewrite_result.action)
  println(rewrite_result.command)
  println(rewrite_result.original_command)
  println(rewrite_result.result.ran)
  let rewrite_audits = lifecycle_audit_log_take()
  /* Expect two audit entries: tool_rewrite + tool_hooks.reminder_injected. */
  println(len(rewrite_audits))
  println(rewrite_audits[0].kind)
  println(rewrite_audits[0].payload.rule_id)
  println(rewrite_audits[0].payload.original_command)
  println(rewrite_audits[0].payload.command)
  println(rewrite_audits[1].kind)
  println(rewrite_audits[1].payload.tags[0])
  println(rewrite_audits[1].payload.ttl_turns)
  println(contains(rewrite_audits[1].payload.body, "rewritten"))
  println(contains(rewrite_audits[1].payload.body, "target-shared"))
  let deny_rule = {
    rule_id: "custom.git_push_force",
    catalogue_id: "preset_run_command/custom",
    severity: "error",
    explanation: "Use --force-with-lease",
    references: [],
    rewrite: nil,
  }
  let deny_result = tool_hooks_mode_deny_with_explanation(deny_rule, {command: "git push origin main --force"}, echo)
  println(deny_result.action)
  println(deny_result.rule_id)
  /* Deny must not call `inner` — the echo executor's `ran` field
   * should be absent from the result envelope. */
  println(deny_result?.result == nil)
  let deny_audits = lifecycle_audit_log_take()
  println(len(deny_audits))
  println(deny_audits[0].kind)
  println(deny_audits[0].payload.rule_id)
  println(deny_audits[0].payload.command)
  println(deny_audits[0].payload.severity)
  let warn_rule = {
    rule_id: "python.find_versus_rg",
    catalogue_id: "harn-canon/python",
    severity: "info",
    explanation: "rg --files is much faster",
    references: [],
    rewrite: nil,
  }
  let warn_result = tool_hooks_mode_passthrough_only_audit(warn_rule, {command: "find . -name foo"}, echo)
  println(warn_result.action)
  println(warn_result.command)
  println(warn_result.result.ran)
  let warn_audits = lifecycle_audit_log_take()
  println(len(warn_audits))
  println(warn_audits[0].kind)
  println(warn_audits[0].payload.rule_id)
  println(warn_audits[0].payload.severity)
  /* Passthrough must not inject a reminder — only the warning audit
   * entry should be present. */
  println(warn_audits[0].kind == "tool_rule_warning")
  /* Rewrite mode without an `inner` returns the envelope alone but
   * still records the audit + reminder side effects. The
   * `rule.rewrite` callback is invoked so the envelope reflects the
   * rewritten command. */
  let preview = tool_hooks_mode_rewrite_with_audit(rewrite_rule, "cargo test", nil)
  println(preview.action)
  println(preview.command)
  println(preview.original_command)
  println(preview?.result == nil)
  let preview_audits = lifecycle_audit_log_take()
  println(len(preview_audits))
  println(preview_audits[0].kind)
  println(preview_audits[1].kind)
}
