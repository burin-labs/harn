pipeline test(harness: Harness, task: unknown) {
  const yaml = yaml_parse("name: Ada\nage: 37\n")
  const toml_data = toml_parse("name = \"Ada\"\nage = 37\n")
  harness.stdio.log(yaml.name)
  harness.stdio.log(toml_data.age)
  harness.stdio.log(json_stringify(yaml_parse(yaml_stringify({name: "Ada", age: 37}))))
  harness.stdio.log(json_stringify(toml_parse(toml_stringify({name: "Ada", age: 37}))))
}
