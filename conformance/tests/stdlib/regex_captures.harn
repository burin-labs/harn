pipeline test(harness: Harness, task) {
  // Basic capture groups
  const caps = regex_captures("(\\w+)@(\\w+)", "user@host admin@server")
  harness.stdio.println(len(caps))
  harness.stdio.println(caps[0]["match"])
  harness.stdio.println(caps[0]["groups"])
  harness.stdio.println(caps[1]["match"])
  harness.stdio.println(caps[1]["groups"])
  // Named capture groups
  const named = regex_captures("(?P<name>\\w+):(?P<value>\\d+)", "foo:42 bar:99")
  harness.stdio.println(named[0]["name"])
  harness.stdio.println(named[0]["value"])
  harness.stdio.println(named[1]["name"])
  harness.stdio.println(named[1]["value"])
  // No matches returns empty list
  const none = regex_captures("(\\d+)", "no digits here")
  harness.stdio.println(none)
  // Optional groups (non-matching)
  const opt = regex_captures("(\\w+)(-(\\w+))?", "hello")
  harness.stdio.println(opt[0]["match"])
  harness.stdio.println(opt[0]["groups"])
}
