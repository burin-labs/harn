pipeline default(harness: Harness) {
  const base = date_parse("2026-04-25T00:00:00Z")
  const later = date_add(base, duration_hours(2))
  harness.stdio.println(date_to_zone(later, "UTC"))
  harness.stdio.println(duration_to_seconds(date_diff(later, base)))
  harness.stdio.println(duration_to_human(duration_minutes(194)))
  harness.stdio.println(duration_to_human(date_diff(base, later)))
  harness.stdio.println(date_format(date_add(base, duration_ms(1500)), "%H:%M:%S.%3f"))
}
