pipeline default() {
  mock_time(1000)
  let receipt = emit_channel("demo.created", {n: 1}, {id: "demo-1", ttl: 5s})
  let events = channel_events("demo.created")
  println(receipt.name_resolved)
  println(receipt.scope)
  println(receipt.event_id)
  println(receipt.duplicate)
  println(receipt.emitted_at.at_ms)
  println(starts_with(receipt.emitted_at.signature, "sha256:"))
  println(receipt.ttl_ms)
  println(len(events))
  println(events[0].payload.n)
  println(events[0].id)
  println(events[0].ttl_ms)
  let duplicate = emit_channel("demo.created", {n: 2}, {id: "demo-1"})
  println(duplicate.event_id == receipt.event_id)
  println(duplicate.duplicate)
  println(channel_events("demo.created")[0].payload.n)
  unmock_time()
}
