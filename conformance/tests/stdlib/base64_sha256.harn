pipeline test(harness: Harness, task: unknown) {
  // base64 encode/decode
  const encoded = base64_encode("hello world")
  harness.stdio.log(encoded)
  const decoded = base64_decode(encoded)
  harness.stdio.log(decoded)
  const bytes = bytes_from_string("hello world")
  harness.stdio.log(type_of(bytes))
  harness.stdio.log(bytes_to_string(bytes))
  harness.stdio.log(bytes_to_hex(bytes_from_hex("0001ff")))
  harness.stdio.log(bytes_to_base64(bytes_from_string("hi")))
  harness.stdio.log(bytes_to_string(bytes_from_base64("aGk=")))
  // sha256
  const hash = sha256("hello")
  harness.stdio.log(hash)
  // md5
  const md = md5("hello")
  harness.stdio.log(md)
}
