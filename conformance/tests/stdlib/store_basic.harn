pipeline default() {
  // Start clean
  store_clear()
  // Set and get
  store_set("name", "alice")
  store_set("age", 30)
  __io_println(store_get("name"))
  __io_println(store_get("age"))
  // Get missing key
  __io_println(store_get("missing"))
  // List keys
  let keys = store_list()
  __io_println(len(keys))
  // Delete
  store_delete("age")
  __io_println(store_get("age"))
  __io_println(len(store_list()))
  // Complex values
  store_set("config", {theme: "dark", size: 14})
  let cfg = store_get("config")
  __io_println(cfg.theme)
  __io_println(cfg.size)
  // Clear
  store_clear()
  __io_println(len(store_list()))
}
