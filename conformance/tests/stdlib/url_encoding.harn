pipeline default(task) {
  // Basic URL encoding
  let encoded = url_encode("hello world")
  __io_println(encoded)
  // Round-trip
  let decoded = url_decode(encoded)
  __io_println(decoded)
  // Special characters
  __io_println(url_encode("a=1&b=2"))
  __io_println(url_decode("a%3D1%26b%3D2"))
  // Plus decoding (form-encoded space)
  __io_println(url_decode("hello+world"))
  // Unreserved chars pass through
  __io_println(url_encode("abc-123_test.file~ok"))
}
