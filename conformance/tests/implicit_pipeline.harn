// Test: top-level code executes without a pipeline block
let x = 1 + 2

println(x)

fn double(n) {
  return n * 2
}

println(double(5))

let items = [1, 2, 3]

for item in items {
  print("${item} ")
}

println("")
