pipeline test(task) {
  const name = "world"
  const greeting = """
    hello ${name}!
    """
  log(greeting)
  const x = 10
  const y = 20
  const report = """
    sum: ${x + y}
    product: ${x * y}
    """
  log(report)
}
