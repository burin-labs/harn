pipeline test(harness: Harness, task) {
  // starts_with
  harness.stdio.log(starts_with("hello world", "hello"))
  harness.stdio.log(starts_with("hello world", "world"))
  // ends_with
  harness.stdio.log(ends_with("hello world", "world"))
  harness.stdio.log(ends_with("hello world", "hello"))
  // contains (string)
  harness.stdio.log(contains("hello world", "lo wo"))
  harness.stdio.log(contains("hello world", "xyz"))
  // contains (list)
  harness.stdio.log(contains([1, 2, 3], 2))
  harness.stdio.log(contains([1, 2, 3], 5))
  // replace
  harness.stdio.log(replace("hello world", "world", "harn"))
  // join
  harness.stdio.log(join(["a", "b", "c"], ", "))
  harness.stdio.log(join(["x"], "-"))
  // len
  harness.stdio.log(len("hello"))
  harness.stdio.log(len([1, 2, 3]))
  harness.stdio.log(len({}))
  // substring (start, end?) — second arg is an exclusive end index
  harness.stdio.log(substring("hello world", 0, 5))
  harness.stdio.log(substring("hello world", 6))
  harness.stdio.log(substring("hello world", 6, 9))
  // dirname
  harness.stdio.log(dirname("/usr/local/bin/harn"))
  // basename
  harness.stdio.log(basename("/usr/local/bin/harn"))
  // extname
  harness.stdio.log(extname("file.txt"))
  harness.stdio.log(extname("archive.tar.gz"))
  harness.stdio.log(extname("noext"))
}
