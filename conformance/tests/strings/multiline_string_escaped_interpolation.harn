pipeline test(task) {
  const name = "world"
  const single = "\${VAR}"
  log("single=${single}")
  const multi = """
    shell \${VAR:-default}
    harn ${name}
    """
  log(multi)
  const escaped_only = """
    only \${VAR}
    """
  log(escaped_only)
}
