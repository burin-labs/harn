pipeline test(harness: Harness, task: unknown) {
  harness.stdio.log("hello world".replace("world", "harn"))
  harness.stdio.log("a,b,c".split(","))
  harness.stdio.log("  hello  ".trim())
  harness.stdio.log("hello".starts_with("hel"))
  harness.stdio.log("hello".starts_with("xyz"))
  harness.stdio.log("hello".ends_with("lo"))
  harness.stdio.log("hello".ends_with("xy"))
  harness.stdio.log("hello".substring(1, 3))
  // Negative / out-of-range indices clamp to [0, len]; end < start yields "".
  harness.stdio.log("[" + "hello world".substring(0, -1) + "]")
  harness.stdio.log("[" + "hello world".substring(-3, 5) + "]")
  harness.stdio.log("[" + "hello world".substring(2, -3) + "]")
  harness.stdio.log("[" + "hello world".substring(0, 100) + "]")
}
