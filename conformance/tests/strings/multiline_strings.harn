pipeline test(harness: Harness, task) {
  // Basic multiline string
  const s = """
    hello
    world
    """
  harness.stdio.println(s)
  // Multiline preserves internal newlines
  const multi = """
    line1
    line2
    line3
    """
  harness.stdio.println(len(multi.split("\n")))
  // Empty multiline
  const empty = ""
  harness.stdio.println(len(empty))
}
