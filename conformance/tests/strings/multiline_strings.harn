pipeline test(task) {
  // Basic multiline string
  const s = """
    hello
    world
    """
  log(s)
  // Multiline preserves internal newlines
  const multi = """
    line1
    line2
    line3
    """
  log(len(multi.split("\n")))
  // Empty multiline
  const empty = ""
  log(len(empty))
}
