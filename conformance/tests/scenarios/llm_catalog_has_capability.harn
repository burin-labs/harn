// std/llm/catalog.has_capability returns true when the model supports
// the given capability per the runtime catalog and false otherwise.
import { has_capability } from "std/llm/catalog"

pipeline default() {
  const m = "claude-sonnet-4-20250514"
  __io_println(has_capability(m, "native_tools"))
  __io_println(has_capability(m, "prompt_caching"))
  __io_println(has_capability(m, "vision"))
  __io_println(has_capability(m, "files_api"))
  __io_println(has_capability(m, "thinking"))
  __io_println(has_capability(m, "tool_search"))
  // unknown capability name → false
  __io_println(has_capability(m, "telekinesis"))
  // unknown model → no catalog entry → all defaults false
  __io_println(has_capability("totally-unknown-model-xyz", "vision"))
  __io_println(has_capability("totally-unknown-model-xyz", "tool_search"))
}
