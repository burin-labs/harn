@persona(name: "merge_captain")
fn merge_pr(pr: string) {
  return admin_merge(pr)
}

@step(name: "admin_merge")
fn admin_merge(pr: string) {
  return "body:" + pr
}

pipeline default(harness: Harness) {
  const result = merge_pr("PR-9")
  harness.stdio.log(result)
}
