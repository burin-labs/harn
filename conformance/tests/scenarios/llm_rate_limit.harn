pipeline test(task) {
  // Query with no limit configured -> nil
  const none = llm_rate_limit("test_provider_xyz")
  log(none)
  // Set a limit
  llm_rate_limit("test_provider_xyz", {rpm: 100})
  const limit = llm_rate_limit("test_provider_xyz")
  log(limit)
  // Clear it
  llm_rate_limit("test_provider_xyz", {rpm: 0})
  const cleared = llm_rate_limit("test_provider_xyz")
  log(cleared)
}
