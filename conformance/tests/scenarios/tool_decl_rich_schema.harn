pipeline test(harness: Harness, task) {
  tool create_user(
    user: {name: string, age?: int},
    tags: list<string>,
    prefs: dict<string, int>,
  ) -> {ok: bool, names: list<string>} {
    return {ok: user.name != "" && prefs["missing"] == nil, names: tags}
  }
  const tool_def = tool_find(create_user, "create_user")
  harness.stdio.println(tool_def.parameters.user.type)
  harness.stdio.println(tool_def.parameters.user.properties.name.type)
  harness.stdio.println(tool_def.parameters.user.required[0])
  harness.stdio.println(tool_def.parameters.tags.items.type)
  harness.stdio.println(tool_def.parameters.prefs.additionalProperties.type)
  harness.stdio.println(tool_def.outputSchema.properties.ok.type)
  harness.stdio.println(tool_def.outputSchema.properties.names.items.type)
}
