pipeline test(task) {
  enum Result {
    Ok(value)
    Err(message)
  }
  const success = Result.Ok(42)
  log(success)
  log(success.variant)
  const failure = Result.Err("something went wrong")
  log(failure)
  log(failure.variant)
  log(failure.fields)
}
