pipeline test(harness: Harness, task) {
  const x: int = 42
  harness.stdio.println(x)
  const name: string = "hello"
  harness.stdio.println(name)
  const flag: bool = true
  harness.stdio.println(flag)
}
