import { pick_keys } from "std/collections"

pipeline test(harness: Harness, task: unknown) {
  const counts = {a: 1, b: 2}
  const bad = pick_keys(counts, ["a"], {drop_nil: "yes"})
  harness.stdio.println(bad.a)
}
