pipeline test(harness: Harness, task) {
  struct Pair<A, B> {
    first: A
    second: B
  }
  enum Option<T> {
    Some(value: T)
    None
  }
  const pair: Pair<int, string> = Pair {first: 1, second: "two"}
  const maybe: Option<int> = Option.Some(pair.first)
  harness.stdio.println(pair.first)
  harness.stdio.println(pair.second)
  match maybe {
    Option.Some(value) -> { harness.stdio.println(value) }
    Option.None -> { harness.stdio.println("none") }
  }
}
