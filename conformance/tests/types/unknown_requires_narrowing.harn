// Using an `unknown` value directly as a concrete type must be a type error —
// this is the whole point of `unknown` vs `any`.
pipeline test(harness: Harness, task) {
  const u: unknown = "hello"
  const s: string = u
  harness.stdio.println(s)
}
