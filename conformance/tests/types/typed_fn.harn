pipeline test(harness: Harness, task) {
  fn add(a: int, b: int) -> int {
    return a + b
  }
  harness.stdio.println(add(3, 4))
  fn greet(name: string) -> string {
    return "hello " + name
  }
  harness.stdio.println(greet("world"))
}
