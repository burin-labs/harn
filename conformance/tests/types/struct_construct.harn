pipeline test(task) {
  struct Point {
    x: int
    y: int
  }
  impl Point {
    fn total(self) {
      return self.x + self.y
    }
  }
  let point = Point {x: 3, y: 4}
  __io_println(point.x)
  __io_println(point.y)
  __io_println(point.total())
}
