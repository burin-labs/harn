// `list<int>` cannot flow into `list<float>`: lists are mutable,
// so the implicit covariance that would let a `float` be pushed
// into the underlying `list<int>` would be unsound.
pipeline test(task) {
  let xs: list<int> = [1, 2, 3]
  let ys: list<float> = xs
}
