pipeline test(harness: Harness, task) {
  fn maybe_reset(x: string?, reset: bool) -> string {
    if x != nil {
      if reset {
        x = nil
      }
      return x.upper()
    }
    return ""
  }
  harness.stdio.println(maybe_reset("ok", false))
}
