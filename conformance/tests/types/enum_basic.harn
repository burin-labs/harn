pipeline test(harness: Harness, task) {
  enum Color {
    Red
    Green
    Blue
  }
  const c = Color.Red
  harness.stdio.println(c)
  harness.stdio.println(c == Color.Red)
  harness.stdio.println(c == Color.Blue)
}
