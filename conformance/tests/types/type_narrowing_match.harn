/** Test match arm type narrowing */
pipeline test(harness: Harness, task) {
  fn classify(x: string | int | nil) -> string {
    match x {
      nil -> { return "nil" }
      "hello" -> { return "greeting" }
      42 -> { return "answer" }
      _ -> { return "other" }
    }
  }
  harness.stdio.println(classify(nil))
  harness.stdio.println(classify("hello"))
  harness.stdio.println(classify(42))
  harness.stdio.println(classify("world"))
  harness.stdio.println(classify(7))
}
