pipeline test(harness: Harness, task) {
  enum Status {
    Active
    Inactive
    Pending(reason)
  }
  const s = Status.Pending("waiting for approval")
  match s.variant {
    "Active" -> { harness.stdio.log("active") }
    "Inactive" -> { harness.stdio.log("inactive") }
    "Pending" -> { harness.stdio.log("pending: ${s.fields[0]}") }
  }
}
