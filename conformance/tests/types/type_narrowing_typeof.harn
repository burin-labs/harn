/** Test type_of() based narrowing */
pipeline test(harness: Harness, task) {
  fn describe(x: string | int | nil) -> string {
    if type_of(x) == "string" {
      return "string: ${x}"
    }
    if type_of(x) == "int" {
      return "int: ${x}"
    }
    return "nil"
  }
  fn not_string(x: string | int) -> string {
    if type_of(x) != "string" {
      return "not string: ${x}"
    }
    return "is string: ${x}"
  }
  harness.stdio.println(describe("hello"))
  harness.stdio.println(describe(42))
  harness.stdio.println(describe(nil))
  harness.stdio.println(not_string(99))
  harness.stdio.println(not_string("hi"))
}
