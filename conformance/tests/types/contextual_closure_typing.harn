fn apply_once(value: int, f: fn(int) -> int) -> int {
  return f(value)
}

pipeline default() {
  const inc: fn(int) -> int = { x -> x + 1 }
  __io_println(inc(5))
  __io_println(apply_once(6, { x -> x + 1 }))
}
