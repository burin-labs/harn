pipeline test(harness: Harness, task) {
  // Typed function should work with correct types
  fn add(a: int, b: int) -> int {
    return a + b
  }
  assert_eq(add(2, 3), 5)
  // Widening: an int satisfies a float parameter. Narrowing does not.
  fn double(x: float) -> float {
    return x * 2
  }
  assert_eq(double(5), 10.0)
  // String param
  fn greet(name: string) -> string {
    return "hi " + name
  }
  assert_eq(greet("world"), "hi world")
  harness.stdio.log("type checks ok")
}
