pipeline default() {
  fn identity<T>(x: T) -> T {
    return x
  }
  __io_println(identity(42))
  __io_println(identity("hello"))
  fn first<T>(items: list<T>) -> T {
    return items[0]
  }
  __io_println(first([10, 20, 30]))
  __io_println(first(["a", "b"]))
}
