fn label(row: tuple<string, int>) -> string {
  return "${row[0]}:${row[1]}"
}

fn sum_values(values: list<int>) -> int {
  let total = 0
  for value in values {
    total += value
  }
  return total
}

pipeline default(harness: Harness) {
  const row = tuple("jobs", 3)
  harness.stdio.println(row[0])
  harness.stdio.println(row[-1])
  const contextual: tuple<string, int> = ["workers", 4]
  harness.stdio.println(label(contextual))
  harness.stdio.println(label(["retries", 2]))
  const [name, count] = row
  harness.stdio.println("${name}:${count}")
  const numbers = tuple(1, 2, 3)
  harness.stdio.println(sum_values(numbers))
  const expanded = tuple(...numbers)
  harness.stdio.println(sum_values(expanded))
}
