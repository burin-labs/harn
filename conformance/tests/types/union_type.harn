pipeline test(harness: Harness, task: unknown) {
  const a: string? = "hello"
  harness.stdio.log(a)
  const b: string? = nil
  harness.stdio.log(b)
  const c: int | float = 3.14
  harness.stdio.log(c)
}
