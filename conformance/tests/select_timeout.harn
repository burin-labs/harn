pipeline test(task) {
  let ch1 = channel("ch1")
  // Don't send anything — timeout should fire
  select {
    msg from ch1 {
      log("got: ${msg}")
    }
    timeout 50ms {
      log("timed out")
    }
  }
}
