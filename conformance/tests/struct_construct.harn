pipeline test(task) {
  struct Point {
    x: int
    y: int
  }
  impl Point {
    fn total(self) {
      return self.x + self.y
    }
  }
  let point = Point {x: 3, y: 4}
  println(point.x)
  println(point.y)
  println(point.total())
}
