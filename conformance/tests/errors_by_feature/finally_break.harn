pipeline test(harness: Harness, task) {
  let cleanup_count = 0
  for i in 0 to 5 exclusive {
    try {
      if i == 2 {
        break
      }
      harness.stdio.println("i: ${i}")
    } finally {
      cleanup_count = cleanup_count + 1
    }
  }
  harness.stdio.println("cleanup_count: ${cleanup_count}")
}
