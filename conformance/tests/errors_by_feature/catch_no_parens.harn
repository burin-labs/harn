/** Test catch without parentheses around error variable */
pipeline default(harness: Harness, task: unknown) {
  // catch e { ... } should work (no parens required)
  try {
    throw "test error"
  } catch (e) {
    harness.stdio.println("caught: " + e)
  }
  // catch (e) { ... } should also still work
  try {
    throw "another error"
  } catch (e2) {
    harness.stdio.println("also caught: " + e2)
  }
  // catch without variable should work too
  try {
    throw "ignored"
  } catch {
    harness.stdio.println("caught without var")
  }
}
