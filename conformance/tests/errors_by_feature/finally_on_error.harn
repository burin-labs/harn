pipeline test(harness: Harness, task) {
  let cleanup_ran = false
  try {
    try {
      throw "error"
    } finally {
      cleanup_ran = true
    }
  } catch (e) {
    harness.stdio.log("outer caught: ${e}")
  }
  harness.stdio.log("cleanup_ran: ${cleanup_ran}")
}
