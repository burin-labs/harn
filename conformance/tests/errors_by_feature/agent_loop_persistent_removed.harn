// `persistent` was removed. Harness authors must choose explicit
// completion looping (`loop_until_done`) and explicit transcript/session
// management.
pipeline test(task) {
  agent_loop("do work", nil, {provider: "mock", persistent: true, max_iterations: 1})
}
