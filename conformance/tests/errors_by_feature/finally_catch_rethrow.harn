pipeline test(harness: Harness, task) {
  let cleanup_ran = false
  try {
    try {
      throw "inner"
    } catch (e) {
      throw "rethrown"
    } finally {
      cleanup_ran = true
    }
  } catch (e2) {
    harness.stdio.println("outer: ${e2}")
  }
  harness.stdio.println("cleanup_ran: ${cleanup_ran}")
}
