// Bounded const-eval — issue #1791. Value-level errors during folding
// (division by zero, integer overflow, out-of-bounds indexing) surface
// at compile time with HARN-CST-004 rather than waiting for the runtime
// to crash.
const ZERO: int = 1 / 0

pipeline main() {
  log(ZERO)
}
