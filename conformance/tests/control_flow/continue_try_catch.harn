pipeline default(harness: Harness, task) {
  let sum = 0
  for i in [1, 2, 3, 4, 5] {
    try {
      if i % 2 == 0 {
        continue
      }
      sum = sum + i
    } catch (e) {
      harness.stdio.log(e)
    }
  }
  harness.stdio.log(sum)
}
