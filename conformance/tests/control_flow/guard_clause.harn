pipeline test(harness: Harness, task) {
  fn process(x) {
    guard x > 0 else {
      return "invalid"
    }
    return "valid: ${x}"
  }
  harness.stdio.println(process(5))
  harness.stdio.println(process(-1))
}
