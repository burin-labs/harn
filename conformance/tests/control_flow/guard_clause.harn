pipeline test(harness: Harness, task: unknown) {
  fn process(x: int) {
    guard x > 0 else {
      return "invalid"
    }
    return "valid: ${x}"
  }
  harness.stdio.log(process(5))
  harness.stdio.log(process(-1))
}
