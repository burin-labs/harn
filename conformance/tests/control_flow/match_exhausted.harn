pipeline test(harness: Harness, task) {
  // Match with no matching arm should throw
  const result = try {
    match "unexpected" {
      "a" -> { "got a" }
      "b" -> { "got b" }
    }
  }
  harness.stdio.println(is_err(result))
  harness.stdio.println(unwrap_err(result))
}
