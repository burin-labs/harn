pipeline test(harness: Harness, task) {
  // Basic guard on literal pattern
  fn classify(n) {
    return match n {
      0 -> { "zero" }
      x if x > 0 -> { "positive" }
      x if x < 0 -> { "negative" }
      _ -> { "unknown" }
    }
  }
  harness.stdio.println(classify(0))
  harness.stdio.println(classify(5))
  harness.stdio.println(classify(-3))
  // Guard with string pattern
  fn greet(name) {
    return match name {
      "admin" -> { "Welcome back, admin!" }
      n if len(n) > 10 -> { "Hello, ${n} (long name)!" }
      n -> { "Hello, ${n}!" }
    }
  }
  harness.stdio.println(greet("admin"))
  harness.stdio.println(greet("Bob"))
  harness.stdio.println(greet("Bartholomew"))
}
