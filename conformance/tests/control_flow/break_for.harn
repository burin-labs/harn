pipeline default(harness: Harness, task: unknown) {
  let result = ""
  for item in ["a", "b", "c", "d", "e"] {
    if item == "d" {
      break
    }
    result = result + item
  }
  harness.stdio.log(result)
}
