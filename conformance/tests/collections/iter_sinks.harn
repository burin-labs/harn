pipeline test(harness: Harness, task) {
  harness.stdio.println(iter([1, 2, 3, 4]).count())
  harness.stdio.println(iter([1, 2, 3, 4]).sum())
  harness.stdio.println(iter([3, 1, 4, 1, 5]).min())
  harness.stdio.println(iter([3, 1, 4, 1, 5]).max())
  harness.stdio.println(iter([1, 2, 3, 4]).reduce(0, { acc, x -> acc + x }))
  harness.stdio.println(iter([10, 20, 30]).first())
  harness.stdio.println(iter([10, 20, 30]).last())
  harness.stdio.println(iter([]).first())
  harness.stdio.println(iter([]).last())
  harness.stdio.println(iter([]).min())
  harness.stdio.println(iter([]).sum())
  harness.stdio.println(iter([1, 2, 3]).any({ x -> x > 2 }))
  harness.stdio.println(iter([1, 2, 3]).any({ x -> x > 99 }))
  harness.stdio.println(iter([1, 2, 3]).all({ x -> x > 0 }))
  harness.stdio.println(iter([1, 2, 3]).all({ x -> x > 1 }))
  harness.stdio.println(iter([1, 2, 3, 4]).find({ x -> x > 2 }))
  harness.stdio.println(iter([1, 2, 3]).find({ x -> x > 99 }))
  iter([1, 2, 3]).for_each({ x -> harness.stdio.println("v=${x}") })
  harness.stdio.println(iter([1, 1, 2, 2, 3]).to_set())
  const d = iter([pair("a", 1), pair("b", 2)]).to_dict()
  harness.stdio.println(d.a)
  harness.stdio.println(d.b)
}
