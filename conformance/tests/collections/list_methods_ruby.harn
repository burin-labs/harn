/** Test Ruby-inspired list methods */
pipeline default(harness: Harness, task) {
  const nums = [1, 2, 3, 4, 5, 6, 7, 8]
  // none
  harness.stdio.println(nums.none({ x -> x > 100 }))
  harness.stdio.println(nums.none({ x -> x > 5 }))
  // every (alias for all)
  harness.stdio.println(nums.every({ x -> x > 0 }))
  harness.stdio.println(nums.every({ x -> x > 5 }))
  // find_index
  harness.stdio.println(nums.find_index({ x -> x > 3 }))
  harness.stdio.println(nums.find_index({ x -> x > 100 }))
  // first / last
  harness.stdio.println(nums.first())
  harness.stdio.println(nums.last())
  harness.stdio.println(nums.first(3))
  harness.stdio.println(nums.last(2))
  // partition
  const parts = nums.partition({ x -> x % 2 == 0 })
  harness.stdio.println(parts.first())
  harness.stdio.println(parts.last())
  // chunk
  harness.stdio.println([1, 2, 3, 4, 5].chunk(2))
  // compact
  harness.stdio.println([1, nil, 2, nil, 3].compact())
  // tally
  harness.stdio.println(["a", "b", "a", "c", "b", "a"].tally())
  // min_by / max_by
  const words = ["hello", "hi", "hey"]
  harness.stdio.println(words.min_by({ w -> len(w) }))
  harness.stdio.println(words.max_by({ w -> len(w) }))
  // group_by — discriminator must be a string (v0.8.43+); coerce explicitly.
  const grouped = [1, 2, 3, 4, 5, 6].group_by({ x -> to_string(x % 2 == 0) })
  harness.stdio.println(grouped)
  // each_cons (sliding window)
  harness.stdio.println([1, 2, 3, 4].each_cons(2))
}
