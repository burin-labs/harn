pipeline test(task) {
  const a = iter([1, 2, 3, 4, 5]).windows(3).to_list()
  log(a)
  const b = iter([1, 2, 3, 4, 5]).windows(5).to_list()
  log(b)
  const c = iter([1, 2, 3]).windows(10).to_list()
  log(c)
  log(len(c))
}
