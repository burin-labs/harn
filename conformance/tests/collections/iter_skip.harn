pipeline test(harness: Harness, task) {
  harness.stdio.println(iter([1, 2, 3, 4, 5]).skip(2).to_list())
  harness.stdio.println(iter([1, 2, 3]).skip(10).to_list())
  harness.stdio.println(iter([1, 2, 3]).skip(0).to_list())
}
