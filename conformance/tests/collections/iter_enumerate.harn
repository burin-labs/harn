pipeline test(harness: Harness, task) {
  const pairs = iter(["a", "b", "c"]).enumerate().to_list()
  for p in pairs {
    harness.stdio.println("${p.first}:${p.second}")
  }
  for (i, x) in iter(["x", "y", "z"]).enumerate() {
    harness.stdio.println("${i}-${x}")
  }
  harness.stdio.println(iter([]).enumerate().to_list())
}
