/** Test set predicate builtins (function-call syntax) */
pipeline default(harness: Harness, task) {
  const a = set(1, 2, 3, 4)
  const b = set(3, 4, 5, 6)
  const small = set(2, 3)
  const disjoint = set(10, 20)
  const empty = set()
  // set_symmetric_difference
  const sd = set_symmetric_difference(a, b)
  harness.stdio.println(len(sd))
  harness.stdio.println(set_contains(sd, 1))
  harness.stdio.println(set_contains(sd, 2))
  harness.stdio.println(set_contains(sd, 5))
  harness.stdio.println(set_contains(sd, 6))
  harness.stdio.println(set_contains(sd, 3))
  harness.stdio.println(set_contains(sd, 4))
  // symmetric difference with self is empty
  harness.stdio.println(len(set_symmetric_difference(a, a)))
  // set_is_subset
  harness.stdio.println(set_is_subset(small, a))
  harness.stdio.println(set_is_subset(a, small))
  harness.stdio.println(set_is_subset(a, a))
  harness.stdio.println(set_is_subset(empty, a))
  // set_is_superset
  harness.stdio.println(set_is_superset(a, small))
  harness.stdio.println(set_is_superset(small, a))
  harness.stdio.println(set_is_superset(a, a))
  harness.stdio.println(set_is_superset(a, empty))
  // set_is_disjoint
  harness.stdio.println(set_is_disjoint(a, disjoint))
  harness.stdio.println(set_is_disjoint(a, b))
  harness.stdio.println(set_is_disjoint(empty, a))
  harness.stdio.println(set_is_disjoint(empty, empty))
}
