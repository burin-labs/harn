pipeline test(harness: Harness, task) {
  // r[k] is O(1) and computes start + k with a bounds check.
  const r = 1 to 10
  harness.stdio.println(r[0])
  harness.stdio.println(r[3])
  harness.stdio.println(r[9])
  // Negative indices count from the end (Python-style).
  harness.stdio.println(r[-1])
  harness.stdio.println(r[-10])
  // Exclusive ranges index the same way
  const e = 0 to 5 exclusive
  harness.stdio.println(e[0])
  harness.stdio.println(e[4])
}
