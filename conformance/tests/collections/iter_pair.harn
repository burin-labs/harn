pipeline test(harness: Harness, task: unknown) {
  const p = pair(1, "hello")
  harness.stdio.log(p.first)
  harness.stdio.log(p.second)
  harness.stdio.log(p)
  const d = {a: 1, b: 2}
  for (k, v) in iter(d) {
    harness.stdio.log("${k}=${v}")
  }
}
