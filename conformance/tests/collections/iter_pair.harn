pipeline test(harness: Harness, task) {
  const p = pair(1, "hello")
  harness.stdio.println(p.first)
  harness.stdio.println(p.second)
  harness.stdio.println(p)
  const d = {a: 1, b: 2}
  for (k, v) in iter(d) {
    harness.stdio.println("${k}=${v}")
  }
}
