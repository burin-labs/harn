pipeline test(harness: Harness, task) {
  const d = {a: 1, b: 2, c: 3}
  harness.stdio.println(d.entries().map({ x -> x.key }))
  harness.stdio.println(d.entries().map({ x -> x.value }))
  harness.stdio.println(d.count)
  const mapped_values = d.map_values({ v -> v * 2 })
  harness.stdio.println(mapped_values.a)
  harness.stdio.println(mapped_values.b)
  harness.stdio.println(mapped_values.c)
  const filtered_dict = d.filter({ v -> v > 1 })
  harness.stdio.println(filtered_dict.has("a"))
  harness.stdio.println(filtered_dict.has("b"))
  harness.stdio.println(filtered_dict.has("c"))
  const d_remove = {a: 1, b: 2, c: 3}
  const without_b = d_remove.removing("b")
  harness.stdio.println(d_remove.has("a"))
  harness.stdio.println(d_remove.has("b"))
  harness.stdio.println(d_remove.has("c"))
  harness.stdio.println(without_b.has("a"))
  harness.stdio.println(without_b.has("b"))
  harness.stdio.println(without_b.has("c"))
  harness.stdio.println(d.get("a"))
  harness.stdio.println(d.get("z", "default_value"))
  harness.stdio.println(d.get("z"))
  const dynamic_key = "b"
  const computed = {a: 1, [dynamic_key]: 2}
  harness.stdio.println(computed.get("a"))
  harness.stdio.println(computed.get("b"))
}
// Should be nil
