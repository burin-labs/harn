pipeline test(harness: Harness, task) {
  fn first_match() {
    for pattern in ["a", "b"] {
      return pattern
    }
    return ""
  }
  let seen = []
  for path in ["outer"] {
    seen = seen + [path + ":" + first_match()]
  }
  harness.stdio.log(join(seen, ","))
}
