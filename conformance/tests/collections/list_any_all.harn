pipeline test(harness: Harness, task) {
  const nums = [2, 4, 6]
  harness.stdio.println(nums.any({ x -> x > 5 }))
  harness.stdio.println(nums.all({ x -> x > 0 }))
  harness.stdio.println(nums.all({ x -> x > 3 }))
}
