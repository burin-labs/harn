pipeline test(harness: Harness, task: unknown) {
  // `to` is inclusive by default
  const nums = 1 to 5
  harness.stdio.log(nums)
  // trailing `exclusive` makes it half-open
  const half = 0 to 3 exclusive
  harness.stdio.log(half)
  // Works in for-in
  for i in 1 to 3 {
    harness.stdio.log(i)
  }
}
