pipeline default(harness: Harness, task) {
  const arr = [10, 20, 30, 40, 50]
  harness.stdio.println(arr[-1])
  harness.stdio.println(arr[-2])
  harness.stdio.println(arr[-5])
  // Out of bounds negative returns nil
  harness.stdio.println(arr[-100])
}
