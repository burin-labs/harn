pipeline default(harness: Harness, task: unknown) {
  const arr = [10, 20, 30, 40, 50]
  harness.stdio.log(arr[-1])
  harness.stdio.log(arr[-2])
  harness.stdio.log(arr[-5])
  // Out of bounds negative returns nil
  harness.stdio.log(arr[-100])
}
