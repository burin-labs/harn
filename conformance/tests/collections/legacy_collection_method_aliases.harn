pipeline test(harness: Harness, task) {
  const values = [3, 1, 2]
  harness.stdio.println(values.push(4))
  harness.stdio.println(values.pop())
  harness.stdio.println(values.sort())
  harness.stdio.println(values.sort_by({ value -> 0 - value }))
  harness.stdio.println(values.reverse())
  harness.stdio.println("harn".reverse())
  const original_set = set([1, 2])
  harness.stdio.println(original_set.add(3).to_list().sorted())
  harness.stdio.println(original_set.remove(1).to_list().sorted())
  harness.stdio.println(original_set.delete(2).to_list().sorted())
  const original_dict = {a: 1, b: 2}
  harness.stdio.println(original_dict.merge({c: 3}))
  harness.stdio.println(original_dict.remove("a"))
  harness.stdio.println(original_dict.rekey({ key -> "x_" + key }))
}
