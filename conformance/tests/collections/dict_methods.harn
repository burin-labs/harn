pipeline test(harness: Harness, task) {
  const d = {a: 1, b: 2}
  harness.stdio.println(d.keys())
  harness.stdio.println(d.values())
  harness.stdio.println(d.has("a"))
  harness.stdio.println(d.has("z"))
}
