pipeline test(harness: Harness, task) {
  // .first() and .last() on an inclusive range
  const inc = 1 to 5
  harness.stdio.println(inc.first())
  harness.stdio.println(inc.last())
  // .first() and .last() on an exclusive range
  const exc = 0 to 5 exclusive
  harness.stdio.println(exc.first())
  harness.stdio.println(exc.last())
  // Empty ranges — first/last return nil
  const empty = 5 to 1
  harness.stdio.println(empty.first())
  harness.stdio.println(empty.last())
  harness.stdio.println(empty.len())
  // .contains(x) is O(1)
  harness.stdio.println(inc.contains(3))
  harness.stdio.println(inc.contains(5))
  harness.stdio.println(inc.contains(6))
  harness.stdio.println(exc.contains(0))
  harness.stdio.println(exc.contains(5))
  // .to_list() materializes on demand
  harness.stdio.println((1 to 4).to_list())
  harness.stdio.println((0 to 3 exclusive).to_list())
}
