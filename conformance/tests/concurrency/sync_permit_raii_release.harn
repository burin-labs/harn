fn escaped_lock() {
  return sync_mutex_acquire("raii-return")
}

pipeline test(task) {
  const escaped = escaped_lock()
  const reacquired = sync_mutex_acquire("raii-return", 0)
  __io_println(reacquired != nil)
  __io_println(sync_release(escaped))
  __io_println(sync_release(reacquired))
}
