pipeline test(harness: Harness, task) {
  const ch = harness.runtime.channel("later", 1)
  const h = spawn {
    harness.runtime.receive(ch)
  }
  harness.runtime.yield_now()
  harness.runtime.send(ch, "ok")
  harness.stdio.println(await(h))
}
