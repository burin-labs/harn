pipeline test(task) {
  // Awaiting a task explicitly deregisters it from the nursery, so scope exit
  // neither double-joins nor cancels it.
  scope {
    const h = spawn {
      42
    }
    log(await(h))
  }
  log("done")
}
