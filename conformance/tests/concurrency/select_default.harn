pipeline test(harness: Harness, task) {
  const ch1 = harness.runtime.channel("ch1")
  // Don't send anything — default should fire
  select {
    msg from ch1 {
      harness.stdio.println("got: ${msg}")
    }
    default {
      harness.stdio.println("nothing ready")
    }
  }
}
