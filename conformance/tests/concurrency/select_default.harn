pipeline test(task) {
  const ch1 = channel("ch1")
  // Don't send anything — default should fire
  select {
    msg from ch1 {
      log("got: ${msg}")
    }
    default {
      log("nothing ready")
    }
  }
}
