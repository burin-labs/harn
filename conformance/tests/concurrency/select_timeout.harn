pipeline test(harness: Harness, task) {
  const ch1 = harness.runtime.channel("ch1")
  // Don't send anything — timeout should fire
  select {
    msg from ch1 {
      harness.stdio.log("got: ${msg}")
    }
    timeout 50ms {
      harness.stdio.log("timed out")
    }
  }
}
