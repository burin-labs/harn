pipeline test(task) {
  enum Result {
    Ok(value)
    Err(message)
  }
  let success = Result.Ok(42)
  log(success)
  log(success.variant)
  let failure = Result.Err("something went wrong")
  log(failure)
  log(failure.variant)
  log(failure.fields)
}
