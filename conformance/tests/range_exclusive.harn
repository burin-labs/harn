pipeline test(task) {
  // `to ... exclusive` is the half-open form
  let xs = 0 to 4 exclusive
  log(xs)
  // used in a for loop
  var sum = 0
  for i in 1 to 5 exclusive {
    sum += i
  }
  log(sum)
  // empty when end <= start
  let empty = 5 to 5 exclusive
  log(len(empty))
}
