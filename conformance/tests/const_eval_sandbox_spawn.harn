// Bounded const-eval — issue #1791. Runtime concurrency constructs
// (`spawn`, `parallel`, `select`, `try`, …) are unconditionally rejected
// in a const initializer.
const Z = spawn {
  1
}

pipeline main() {
  log(Z)
}
