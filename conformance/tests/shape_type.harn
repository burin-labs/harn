pipeline test(task) {
  type Config = {model: string, max_tokens: int}

  fn run(config: Config) -> string {
    return config.model
  }

  let cfg = {model: "gpt-4", max_tokens: 100}
  log(run(cfg))
}
