pipeline default(task) {
  const record = {hot: 7, cold: 2, nested: {value: 3}}
  let i = 0
  let total = 0
  while i < 150000 {
    total = total + record.hot + record.nested.value
    i = i + 1
  }
  if total < 0 {
    log("unreachable")
  }
}
