pipeline default(task) {
  let items = [1, 3, 5, 7, 9, 11, 13, 15]
  var i = 0
  var hits = 0
  while i < 100000 {
    if items.contains(i % 16) {
      hits = hits + 1
    }
    i = i + 1
  }
  if hits < 0 {
    log("unreachable")
  }
}
