Interrupting `make check-docs-snippets` no longer leaves staged snippets as
untracked repository state. The checker stages into the gitignored workspace
scratch root (`.harn-tmp/`) instead of creating `harn-docs-snippets-*` in the
repository root, and removes its own directory through the filesystem
capability rather than shelling out to `rm -rf`.
