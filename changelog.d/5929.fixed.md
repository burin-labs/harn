- **Whole-program capability fixes preserve explicit split handles (#5929).**
  Functions that already accept separate narrow capability parameters keep that
  signature and their call sites unchanged instead of being collapsed into an
  invalid record parameter with duplicated arguments.
