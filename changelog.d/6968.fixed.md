Reuse the exact receipt-verified Harn CLI binary after native Windows tests instead of rebuilding it before the smoke
test. Nextest now keeps default run and worktree artifacts inside each isolated test attempt instead of polluting the
source checkout and invalidating that receipt.
