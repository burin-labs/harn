Exported Harn functions now advertise a closed input object, so an unknown named
argument is rejected as a schema violation instead of being silently dropped on
its way to the handler. A component-backed tool registry is now covered end to
end on both the generated CLI and MCP, including an out-of-range input that must
not reach the handler and an out-of-contract result that must not be reported as
a success.
