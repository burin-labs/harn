- **Typed `harn-tools/1.0` contract and offline export catalog (#7752).** Publish
  the JSON Schema and strict TypeScript contract, and let `harn tool schema
  --surface exports` derive the same catalog from public Harn functions without
  running `main`.
