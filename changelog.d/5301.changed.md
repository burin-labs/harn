- **CI executes the Rust suite from an exact-SHA nextest archive (#5301).**
  The behavior producer compiles the environment-neutral suite once and ships
  digest-bound test and Landlock archives. Consumers verify the source,
  toolchain, nextest version, build flags, members, checksums, and size budget
  before execution, eliminating a duplicate cold workspace build.
