`harn fix --capability-migrations-only` no longer adds a capability parameter
to a callable whose value escapes. A tool registry stores `handler: some_handler`
and later calls `handler(args)` through that reference, so a widened signature
breaks the call while leaving no static call site for the type checker to
reject. Such a callable now keeps its signature and its unmet requirement stays
visible as a diagnostic.
