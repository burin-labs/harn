- **Test and run timing now expose typed distributions and module-phase
  attribution (#4824).** User-test JSON schema v2 includes timeout, per-case
  phase, aggregate, and p50/p90/p95/p99 data; conformance JSON and the resident
  test worker expose the same typed distribution; `harn time run` and run phase
  schema v2 add VM-scoped module compile/load rows without ambient global timing
  state.
