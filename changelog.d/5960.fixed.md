Package verification now builds and resolves the exact-tree Harn CLI once,
reuses it for every typed package inspection, and generates the release AOT
payload against that warm dependency graph instead of paying two serial cold
builds.
