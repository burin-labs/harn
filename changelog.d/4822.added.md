Discover installed package personas through qualified `persona list` and `persona inspect` results,
package reports, and package diagnostics. Explicitly activate them per project with attenuation-only
authority, durable policy pins, typed activation/deactivation receipts, and digest-valid qualified
runtime trigger bindings that remain inert until activation.
