An agent run's final assistant text is now the model's prose alone. When a
provider returned the reply as typed content blocks, the whole block list was
rendered into `result.text` / `result.visible_text` as an unparseable dump that
carried the model's private reasoning and its opaque signatures into the field
consumers read as the assistant's answer. Only text blocks reach that field
now; everything else stays in the transcript.
