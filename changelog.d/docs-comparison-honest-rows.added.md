Added three rows to the feature matrix: sandboxed-by-default execution, signed
and versioned packages, and install size. The install-size row is one Harn does
not win, and carries measured download sizes for the current release alongside
the issue tracking the Windows packaging defect behind its outlier number.
