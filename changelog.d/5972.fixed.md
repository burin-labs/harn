First-party connector packages compatible with Harn 0.10 can continue importing
the connector HTTP helpers from `std/connectors/shared`; that established module
now re-exports the single implementation owned by `std/connectors/http`.
