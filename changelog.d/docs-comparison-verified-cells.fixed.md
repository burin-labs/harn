Corrected two sandbox ratings on the comparison page against primary sources.
Temporal's Python workflow sandbox is documented as a determinism boundary that
is not completely isolated, and Cursor's cloud agents run in per-agent
Firecracker microVMs, which the previous note understated.
