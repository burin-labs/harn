Add a typed Agent Plugins 1.0 package loader, `harn plugin inspect` and
`harn plugin validate`, isolated skill and MCP diagnostics, safe plugin path
expansion, and static HTTP header projection into Harn's MCP runtime.
