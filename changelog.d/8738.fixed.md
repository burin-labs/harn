On Windows the command tool's children now run inside the AppContainer. They used to be launched through a
`Command`, which cannot carry one, so they ran with no filesystem confinement. A policy's container is now named
by what it is granted and reused, so a workspace pays its recursive ACL grant once instead of twice per spawn.
