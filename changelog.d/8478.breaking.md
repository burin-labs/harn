- **Generated session-update bindings follow the emitted metadata envelope (#8478).** Extension fields live in typed
  `_meta.harn` records, derived from the owning ACP schema. Rust `ACPTypedSessionUpdate` and Swift
  `HarnACPTypedSessionUpdate` decode the advertised variants and reject missing or invalid identity. Root-field payload
  shapes and the obsolete `HARN_TYPED_SESSION_UPDATE_PAYLOADS` field-table constant are removed; use the typed union and
  the schema instead. Standard `available_commands_update.availableCommands` remains at the update root.
