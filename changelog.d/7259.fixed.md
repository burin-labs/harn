- **Generated catalogs no longer trigger redundant Harn rebuilds (#7259).**
  Multi-format catalog commands now reuse one resolved executable and publish
  their outputs only after every projection succeeds. Dev setup also avoids a
  ghost portal asset that forced freshness recovery on the first real build.
