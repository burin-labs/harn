- **`harn fix` capability migrations no longer trust stale diagnostic byte
  offsets (#5850).** `bindings/prepend-capability-argument` now resolves the
  insertion point from the diagnostic's file-level line/column against the
  current source, so a second capability-migration pass cannot splice into an
  earlier import when typechecking reuses an interpolation-local offset.
