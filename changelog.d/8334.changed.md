The Rust workspace test job now prefers the owned Linux pool on same-repository pull requests, falling back to GitHub-hosted capacity whenever the fleet census reports no carriers or cannot be read. Merge groups and main pushes are unchanged.
