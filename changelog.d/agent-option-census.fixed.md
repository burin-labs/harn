Repository checks recognize model options inventoried under their owning categories.
The option census also covers the caller's schema-validation boundary without hiding
future literal reads inside its helper.
