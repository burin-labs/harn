Give the no-progress recovery ladder a second rung. A prose-only stall used to
get exactly one corrective turn before the loop reported a terminal stuck stop,
and that corrective was appended to a transcript already dominated by the
model's own repeated narration, so the same request produced the same
narration. The escalated rung now drops the repeated narration turn before
re-asking, carrying the stated intent forward inside the corrective instead.
The recovery receipt reports `rung`, `rung_budget`, and `dropped_narration`, and
the loop still reports a terminal stuck stop once every rung is spent.
