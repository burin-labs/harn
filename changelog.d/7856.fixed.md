CI latency reports now separate dependency wait, runner queue, execution, and
post-job tail. Reports retain per-run terminal-path evidence and fail closed
when executed jobs have malformed or missing timestamps.
