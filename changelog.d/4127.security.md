- **Agent subprocess sandboxing now defaults to a required OS sandbox
  carrier, and Linux seccomp uses a default-deny syscall allowlist
  (#4127).** Top-level agent loops install an `os_hardened` carrier by
  default, and Linux subprocesses omit process-introspection, `io_uring`,
  and addressable socket syscalls unless policy permits them.
