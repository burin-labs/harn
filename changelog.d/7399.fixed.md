Unchanged durable reminder-provider output with a stable `dedupe_key` is no
longer committed again on every subscribed agent lifecycle event. Harn retains
the active reminder atomically, reports the provider evaluation as skipped,
and still emits changed or expired directives as new durable history.
