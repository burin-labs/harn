The Harn binary resolver no longer amplifies compiler-wrapper contention by
silently launching a second cold build after a timeout. Wrapper-disabled retry
is now explicit through `HARN_BIN_RETRY_WITHOUT_WRAPPER=1`, with actionable
timeout diagnostics for waiting longer or reusing a built binary.
