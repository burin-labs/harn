Expose a typed tool-permission evaluator so native hosts can delegate Harn-owned
request normalization, matcher taxonomy, and decision precedence while retaining
native policy persistence and approval presentation.
