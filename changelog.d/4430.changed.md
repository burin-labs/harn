- Persona scaffolding now compiles from the shipped canonical template assets,
  validates the exact entry workflow and smoke suite before publication, and
  restores forced replacements if publication fails.
