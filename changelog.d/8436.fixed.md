- **AWS Smithy types now resolve across the whole compatible 1.6 line
  (#8436).** The published `cloud-aws` feature bounded Smithy types to one
  exact release, which blocked patch fixes and desynchronized from the
  package-verify minimum whenever a dependency update rewrote it. The bound is
  now the range the incompatibility actually describes: 1.6.2 or later, below
  1.7.
