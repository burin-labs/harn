- **ACP/A2A artifact manifest surfaces.** Adapter coverage now pins
  `artifact_manifest` session and task artifact updates so document bundles keep
  their manifest specs, MIME type, fallback text, provenance, and metadata intact
  across protocol bridges (#4134).
