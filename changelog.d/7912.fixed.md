A provider call that recovered from a zero-token empty completion now reports
the cost it measured. The discarded attempt no longer counts as an unpriced
call, so a recovered retry stops nulling the call's cost and stops making a
cost governor consume its whole ceiling.
