A repository gate now budgets the stack frames a nested agent, tool, or
sub-agent descent re-enters, so a frame that grows toward the ceiling is
refused at review time rather than discovered when a worker stack overflows.
