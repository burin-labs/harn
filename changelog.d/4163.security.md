- **Provider auth secret references (#4163).** Provider API-key env vars may now
  point at Harn secret references such as `harn-secret://provider/anthropic-api-key`;
  Harn resolves the raw key only inside provider auth construction and catalog
  availability checks understand the same reference form.
