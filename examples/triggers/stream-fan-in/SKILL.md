---
name: stream-fan-in
short: Customize a stream fan-in trigger pipeline.
description: Stream trigger example for routing or grouping event batches.
when-to-use: Use when consuming stream-shaped trigger events.
---
# Stream fan-in

Choose the stream provider in `harn.toml`, then customize `lib.harn` to group,
classify, or route events.
