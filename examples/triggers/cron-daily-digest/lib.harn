import "std/triggers"

/** Emit a weekday digest placeholder for a cron tick. */
pub fn send_daily_digest(harness: Harness, event: TriggerEvent) {
  harness.stdio.println("running digest for ${event.kind}")
}
