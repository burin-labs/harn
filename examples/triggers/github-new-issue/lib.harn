import "std/triggers"

/** Return true when a trigger event came from the GitHub provider. */
pub fn should_handle(event: TriggerEvent) -> bool {
  return event.provider == "github"
}

/** Log the issue triage handoff for a new GitHub issue. */
pub fn on_new_issue(harness: Harness, event: TriggerEvent) {
  harness.stdio.log("triaging ${event.kind}")
}
