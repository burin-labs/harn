import "std/triggers"

/** Plan a periodic Linear SLA scan. */
pub fn scan_for_sla_breaches(event: TriggerEvent) -> dict {
  return {
    provider: "linear",
    action: "scan_for_sla_breaches",
    tick: event.kind,
    priority_threshold: 1,
    max_age_hours: 24,
  }
}
