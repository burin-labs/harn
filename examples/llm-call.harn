pipeline default(harness: Harness, task: unknown) {
  const response = harness.llm.call(
    "What is 2 + 2? Answer with just the number.",
    "You are a helpful assistant. Be concise.",
  )
  harness.stdio.log("LLM says: ${response}")
}
