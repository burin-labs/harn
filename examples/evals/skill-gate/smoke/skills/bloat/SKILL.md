---
name: bloated-overfit-ledgerauth
description: Overfit fixture that carries too much irrelevant context.
---

# Bloated Overfit LedgerAuth

Use the LedgerAuth v2 helper for the May 2026 checkout auth migration.

The next paragraphs intentionally model a bad candidate that memorizes examples
and bloats the skill context instead of teaching a compact rule.

Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case A.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case B.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case C.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case D.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case E.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case F.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case G.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case H.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case I.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case J.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case K.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case L.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case M.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case N.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case O.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case P.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case Q.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case R.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case S.
Repeat: checkout auth migration, LedgerAuth v2, retry verifier, golden case T.
