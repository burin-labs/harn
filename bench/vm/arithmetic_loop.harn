pipeline default(harness: Harness, task) {
  let i = 0
  let total = 0
  while i < 200000 {
    total = total + (i + 3) * 2 - 1
    i = i + 1
  }
  if total < 0 {
    harness.stdio.log("unreachable")
  }
}
