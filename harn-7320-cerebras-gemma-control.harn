pipeline default(harness: Harness, task: unknown) {
  const result = harness.llm.call_safe(
    "Reply with exactly PONG.",
    nil,
    {
      provider: "cerebras",
      model: "gemma-4-31b",
      stream: true,
      max_tokens: 16,
      retries: 0,
      timeout_ms: 30000,
    },
  )
  harness.stdio.println(json_stringify(result))
}
