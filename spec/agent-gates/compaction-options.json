[
  {
    "default": "absent; required only by the explicit classify strategy",
    "evidence": "autocompact.__compact_options forwards this typed classification configuration to the shared compaction owner unless auto_compact already supplies it. compaction_classify.harn reaches this exact wrapper and asserts applied decisions and preserved source.",
    "expiry": "2026-10-22",
    "kind": "classification policy",
    "layer": "runner",
    "name": "AgentSpec.compaction.classify",
    "reachability": "yes",
    "readers": [
      {
        "file": "crates/harn-stdlib/src/stdlib/agent/autocompact.harn",
        "reader": "__compact_options"
      }
    ],
    "reason": "Keep explicit evaluator and confidence-floor authority; fixture evidence establishes application semantics, not model quality or default promotion.",
    "verdict": "KEEP-with-expiry"
  },
  {
    "default": "true within an explicitly supplied auto_compact object",
    "evidence": "autocompact.__compaction_enabled disables an object configuration only for explicit enabled=false; absent compaction configuration does not enable compaction.",
    "expiry": "2026-09-13",
    "kind": "boolean option",
    "layer": "runner",
    "name": "AgentSpec.auto_compact.enabled",
    "reachability": "unknown",
    "readers": [
      {
        "file": "crates/harn-stdlib/src/stdlib/agent/autocompact.harn",
        "reader": "__compaction_enabled"
      }
    ],
    "reason": "Retain the compaction activation contract until the caller and native compaction policy are consolidated.",
    "verdict": "KEEP-with-expiry"
  },
  {
    "default": "hybrid for an object",
    "evidence": "autocompact.__compaction_strategy_label reads strategy from an object; a string configuration is already the strategy label.",
    "expiry": "2026-09-13",
    "kind": "strategy",
    "layer": "runner",
    "name": "AgentSpec.compaction.strategy",
    "reachability": "unknown",
    "readers": [
      {
        "file": "crates/harn-stdlib/src/stdlib/agent/autocompact.harn",
        "reader": "__compaction_strategy_label"
      },
      {
        "file": "crates/harn-stdlib/src/stdlib/agent/autocompact.harn",
        "reader": "__compaction_enabled"
      }
    ],
    "reason": "Keep one canonical strategy field while retiring duplicate spellings.",
    "verdict": "KEEP-with-expiry"
  },
  {
    "default": "compact_keep_last, then engine default",
    "evidence": "autocompact.__compaction_keep_last accepts an integer under keep_last_n before the top-level compact_keep_last.",
    "expiry": "2026-09-13",
    "kind": "retention limit",
    "layer": "runner",
    "name": "AgentSpec.compaction.keep_last_n",
    "reachability": "unknown",
    "readers": [
      {
        "file": "crates/harn-stdlib/src/stdlib/agent/autocompact.harn",
        "reader": "__compaction_keep_last"
      }
    ],
    "reason": "Retain the primary field until compaction request normalization has one owner.",
    "verdict": "KEEP-with-expiry"
  }
]
