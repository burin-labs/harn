{
  "$comment": "Generated by `make gen-portable-benchmark-schema`; do not edit by hand.",
  "$defs": {
    "compileMeasurements": {
      "additionalProperties": false,
      "properties": {
        "firstMs": {
          "minimum": 0,
          "type": "number"
        },
        "repeated": {
          "$ref": "#/$defs/compileStatistics"
        }
      },
      "required": [
        "firstMs",
        "repeated"
      ],
      "type": "object"
    },
    "compileStatistics": {
      "allOf": [
        {
          "$ref": "#/$defs/statistics"
        },
        {
          "properties": {
            "iterations": {
              "maximum": 100000
            }
          }
        }
      ]
    },
    "digest": {
      "pattern": "^[0-9a-f]{64}$",
      "type": "string"
    },
    "dispatchMeasurements": {
      "additionalProperties": false,
      "properties": {
        "batchWallMs": {
          "exclusiveMinimum": 0,
          "type": "number"
        },
        "firstMs": {
          "minimum": 0,
          "type": "number"
        },
        "repeated": {
          "$ref": "#/$defs/dispatchStatistics"
        },
        "throughputPerSecond": {
          "exclusiveMinimum": 0,
          "type": "number"
        }
      },
      "required": [
        "firstMs",
        "repeated",
        "batchWallMs",
        "throughputPerSecond"
      ],
      "type": "object"
    },
    "dispatchStatistics": {
      "allOf": [
        {
          "$ref": "#/$defs/statistics"
        },
        {
          "properties": {
            "iterations": {
              "maximum": 1000000
            }
          }
        }
      ]
    },
    "provenance": {
      "additionalProperties": false,
      "properties": {
        "arch": {
          "minLength": 1,
          "type": "string"
        },
        "artifactFormatVersion": {
          "minimum": 1,
          "type": "integer"
        },
        "buildProfile": {
          "enum": [
            "debug",
            "release"
          ]
        },
        "harnVersion": {
          "minLength": 1,
          "type": "string"
        },
        "kernelVersion": {
          "minLength": 1,
          "type": "string"
        },
        "opcodeAbiFingerprint": {
          "$ref": "#/$defs/digest"
        },
        "os": {
          "minLength": 1,
          "type": "string"
        },
        "semanticAbiFingerprint": {
          "$ref": "#/$defs/digest"
        }
      },
      "required": [
        "harnVersion",
        "kernelVersion",
        "artifactFormatVersion",
        "semanticAbiFingerprint",
        "opcodeAbiFingerprint",
        "buildProfile",
        "os",
        "arch"
      ],
      "type": "object"
    },
    "statistics": {
      "additionalProperties": false,
      "properties": {
        "iterations": {
          "minimum": 1,
          "type": "integer"
        },
        "max_ms": {
          "minimum": 0,
          "type": "number"
        },
        "mean_ms": {
          "minimum": 0,
          "type": "number"
        },
        "min_ms": {
          "minimum": 0,
          "type": "number"
        },
        "p50_ms": {
          "minimum": 0,
          "type": "number"
        },
        "p95_ms": {
          "minimum": 0,
          "type": "number"
        },
        "stddev_ms": {
          "minimum": 0,
          "type": "number"
        },
        "total_ms": {
          "minimum": 0,
          "type": "number"
        }
      },
      "required": [
        "iterations",
        "min_ms",
        "mean_ms",
        "p50_ms",
        "p95_ms",
        "max_ms",
        "stddev_ms",
        "total_ms"
      ],
      "type": "object"
    }
  },
  "$id": "https://harnlang.com/schemas/portable-kernel-benchmark.v1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "artifactBytes": {
      "minimum": 1,
      "type": "integer"
    },
    "artifactDigest": {
      "$ref": "#/$defs/digest"
    },
    "compile": {
      "$ref": "#/$defs/compileMeasurements"
    },
    "decode": {
      "oneOf": [
        {
          "$ref": "#/$defs/compileStatistics"
        },
        {
          "type": "null"
        }
      ]
    },
    "dispatch": {
      "$ref": "#/$defs/dispatchMeasurements"
    },
    "entry": {
      "minLength": 1,
      "type": "string"
    },
    "entryKind": {
      "enum": [
        "function",
        "pipeline"
      ]
    },
    "initializationMs": {
      "minimum": 0,
      "type": [
        "number",
        "null"
      ]
    },
    "iterations": {
      "maximum": 1000000,
      "minimum": 1,
      "type": "integer"
    },
    "provenance": {
      "$ref": "#/$defs/provenance"
    },
    "schemaVersion": {
      "const": "harn.portable_kernel.benchmark.v1"
    },
    "source": {
      "minLength": 1,
      "type": "string"
    },
    "target": {
      "enum": [
        "native",
        "browser"
      ]
    },
    "terminalDigest": {
      "$ref": "#/$defs/digest"
    },
    "workers": {
      "maximum": 256,
      "minimum": 1,
      "type": "integer"
    }
  },
  "required": [
    "schemaVersion",
    "target",
    "source",
    "entry",
    "entryKind",
    "artifactBytes",
    "artifactDigest",
    "iterations",
    "workers",
    "provenance",
    "initializationMs",
    "compile",
    "decode",
    "dispatch",
    "terminalDigest"
  ],
  "title": "Harn Portable Kernel benchmark receipt v1",
  "type": "object"
}
