class Counter {
    constructor() {
        this.value = 0;
    }
    increment() {
        this.value += 1;
    }
}

function addOne(n) {
    return n + 1;
}

const square = (n) => n * n;

const VERSION = "1.0";
