{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://harnlang.com/schemas/hostlib/tools/search.response.json",
  "title": "tools.search response",
  "type": "object",
  "properties": {
    "matches": {
      "type": "array",
      "items": { "$ref": "#/$defs/Match" }
    },
    "files_searched": {
      "type": "integer",
      "minimum": 0,
      "description": "Files the walk actually opened and scanned, after the ignore stack and the include/exclude globs. Zero means the request selected no candidates at all, which is a different failure from zero matches across a real candidate set."
    },
    "truncated": {
      "type": "boolean",
      "description": "True when `max_matches` dropped additional matches or `max_line_bytes` clipped any returned match/context line."
    }
  },
  "required": ["matches", "files_searched", "truncated"],
  "additionalProperties": false,
  "$defs": {
    "Match": {
      "type": "object",
      "properties": {
        "path": { "type": "string" },
        "line": { "type": "integer", "minimum": 1 },
        "column": { "type": "integer", "minimum": 1 },
        "text": { "type": "string" },
        "context_before": {
          "type": "array",
          "items": { "type": "string" }
        },
        "context_after": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["path", "line", "column", "text"],
      "additionalProperties": false
    }
  }
}
