import { agent_loop_options } from "std/agent/options"

/** agent_loop. */
pub fn agent_loop(message, system_prompt = nil, options = nil) {
  return __host_llm_session_run(message, system_prompt, agent_loop_options(options))
}
