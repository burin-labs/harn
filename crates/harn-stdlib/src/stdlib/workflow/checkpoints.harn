// @harn-entrypoint-category workflow.stdlib
import { typed_output_checkpoint } from "std/checkpoint"

/**
 * workflow_typed_output_checkpoint.
 *
 * @effects: []
 * @errors: []
 * @api_stability: experimental
 */
pub fn workflow_typed_output_checkpoint(
  agent: HarnessAgent,
  llm: HarnessLlm,
  name: any,
  prompt: any,
  schema: any,
  options: any = nil,
  validator: any = nil,
) {
  return typed_output_checkpoint(agent, llm, name, prompt, schema, options, validator)
}
