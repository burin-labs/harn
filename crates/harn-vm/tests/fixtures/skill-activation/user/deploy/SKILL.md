---
name: deploy
short: User-layer deploy that the project layer shadows
description: Lower-priority duplicate of the project deploy skill
when-to-use: never — the project layer wins this name
---
# Deploy (user)

This duplicate name is shadowed by the project-layer `deploy`.
