---
name: deploy
short: Deploy the service when the user asks for a release
description: Ship the current build to an environment
when-to-use: when the user says deploy or ship a release
---
# Deploy

The project-layer deploy skill. Its body loads only via `load_skill`.
