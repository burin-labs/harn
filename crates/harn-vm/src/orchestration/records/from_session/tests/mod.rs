//! Projection tests driven by session events shaped exactly as a real headless
//! run persisted them, split by the surface each group exercises.

mod support;

mod cost;
mod lifecycle;
mod lineage;
mod projection;
mod tool_calls;
mod transcript;
