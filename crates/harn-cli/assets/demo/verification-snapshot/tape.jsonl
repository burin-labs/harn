{"_note":"intentionally minimal — the verification-snapshot scenario does not call llm_call, but the demo framework requires a non-empty tape so every scenario has a deterministic --llm-mock replay file."}
